node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env — real secrets must NEVER be committed. Use .env.example as the template.
.env
.env.*
!.env.example
!.env.test

# Never commit stray env/notes files (defense-in-depth against past 'test' incident)
/test
/test.*
/secrets*
*.local

# Lockfile discipline — yarn.lock is canonical; ignore npm's to avoid drift
package-lock.json

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
